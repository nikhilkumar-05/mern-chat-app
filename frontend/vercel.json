{
    "outputDirectory": "build",
    "buildCommand": "npm run build",
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "https://mern-chat-app-zkof.onrender.com/api/:path*"
        }
    ]
}